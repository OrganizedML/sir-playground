// grid hier generieren?
class Space {
    constructor(width, height){
        this.widht = width;
        this.heigh = height;
    }

    // add remove uids? -> faster get empty or occupied cells

    get get_neighborhood_empty(position) {
        list = []
        
        // iterate - neighborhood - in range and add positions
        
        return list
    }

    set move_agent(new_position) {
        // do smt
    }
}

export {Space}